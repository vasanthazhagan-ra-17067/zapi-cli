openapi: 3.1.0
info:
  title: Get Channel Categories
  version: "2.0"

paths:
  /api/v2/channels/mycatagories:
    get:
      summary: Retrieves the list of channel categories available to the authenticated user.
      parameters: []
