openapi: 3.1.0
info:
  title: Remove Ephemeral Status
  version: "2.0"

paths:
  /api/v2/statuses/ephemeral:
    delete:
      summary: Removes the currently active ephemeral status for the authenticated user, reverting to the underlying persistent status.
