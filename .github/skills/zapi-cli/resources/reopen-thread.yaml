openapi: 3.1.0
info:
  title: Zoho Cliq – Reopen Thread
  version: "2.0"

paths:
  /v2/threads/{chid}:
    put:
      summary: Reopen a closed thread
      parameters:
        - name: chid
          in: path
          required: true
          description: The unique identifier of the thread (channel/chat ID) to reopen.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - action
              properties:
                action:
                  type: string
                  enum:
                    - reopen
                  description: Action to perform on the thread. Must be "reopen".
            example:
              action: reopen
